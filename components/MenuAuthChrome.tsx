"use client";

import UserFormContainer from "@/src/Container/UserFormContainer";

/** Mounts the login/OTP drawer once for the whole menu app. */
export function MenuAuthChrome() {
  return <UserFormContainer />;
}
